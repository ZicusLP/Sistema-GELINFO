<?xml version = '1.0' encoding = 'utf-8'?>
<!DOCTYPE hibernate-configuration PUBLIC
        "- // Configuração do Hibernate / Hibernate DTD 3.0 // EN"
        "http://www.hibernate.org/dtd/hibernate-configuration-3.0.dtd">

<hibernate-configuration>

    <session-factory>

        <! - Configurações de conexão do banco de dados ->
        <property name = "connection.driver_class"> com.mysql.jdbc.Driver</property>
        <property name = "connection.url"> jdbc:mysql: //localhost:3306/ginfo </property>
        <property name = "connection.username"> root </property>
        <property name = "connection.password"> </property>

        <! - Pool de conexão JDBC (use o integrado) ->
        <property name = "connection.pool_size"> 1 </property>

        <! - dialeto SQL ->
        <property name = "dialect"> org.hibernate.dialect.MySQLInnoDBDialect </property>

        <! - Habilite o gerenciamento automático de contexto de sessão do Hibernate ->
        <property name = "current_session_context_class"> thread </property>

        <! - Desative o cache de segundo nível ->
        <property name = "cache.provider_class"> org.hibernate.cache.internal.NoCacheProvider </property>

        <! - Ecoar todos os SQL executados para stdout ->
        <property name = "show_sql"> true </property>

        <! - Elimine e recrie o esquema do banco de dados na inicialização ->
        <property name = "hbm2ddl.auto"> atualização </property>

     

    </session-factory>

</hibernate-configuration>-8"?>